image:
  repository: caiocfer/gpu-exporter
  tag: v1
  pullPolicy: IfNotPresent

nameOverride: ""
fullnameOverride: ""

serviceAccount:
  create: true
  name: ""

service:
  port: 9835
  annotations:
    prometheus.io/scrape: "true"
    prometheus.io/port: "9835"

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 500m
    memory: 256Mi

nodeSelector: {}

tolerations:
  - key: nvidia.com/gpu
    operator: Exists
    effect: NoSchedule

nvidia:
  # Mounts host NVIDIA library dir and sets LD_LIBRARY_PATH.
  # Prepend container lib path so host libc doesn't shadow the container's.
  enabled: true
  containerLibPath: /lib/x86_64-linux-gnu
  hostLibraryDir: /usr/lib64

extraArgs: {}
