{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "gpu-exporter.serviceAccountName" . }}
  labels:
    {{- include "gpu-exporter.labels" . | nindent 4 }}
{{- end }}
